"""import sqlite3

connection = sqlite3.Connection("user.db")
Cursor = connection.cursor()

 #CREANDO LA TABLA Y LAS COLUMNAS
command = CREATE TABLE IF NOT EXISTS users(name TEXT, last_name TEXT,gmail TEXT, password TEXT)

Cursor.execute(command)"""
